[build]
# O Railway serve o proxy SEFAZ (api/sefaz-proxy.js) e o worker de inbox.
# O frontend React é deployado no Vercel via vercel.json.
buildCommand = "npm install"

[deploy]
startCommand = "bash start.sh"
